machine:
  services:
    - docker

dependencies:
  override:
    - pip install jsonschema
    - pip install git+https://github.com/flywheel-io/gears.git@restructuring

test:
  override:
    - ./bin/process-manifests.sh:
        timeout: 86400
